server {
  listen 80;

  # Configure URL rewrites
  # rewrite ^/old-pricing$ /pricing last;

  # Only needed if there's no CDN in front
  gzip on;

  # Turn on SSI with etags and long values
  ssi on;
  ssi_last_modified on;
  ssi_value_length 1024;

  # All web accessible files live here
  root /site/public;

  # Map error pages
  error_page 404 /404.html;

  location = /data-retention-00.html {
    return 301 /instance-00.html;
  }

  location = /service-messages-00.html {
    return 301 /instance-04-service-messages.html;
  }

  location = /account-settings-01-appearance.htmll {
    return 301 /account-settings-01-appearance.html;
  }

  location = /rest-api/problems/unauthorized {
    return 301 /rest-api/problems/unauthorized.html;
  }

  location = /rest-api/problems/forbidden {
    return 301 /rest-api/problems/forbidden.html;
  }

  location = /rest-api/problems/validation-error {
    return 301 /rest-api/problems/validation-error.html;
  }

  # Folders starting with _ are not publicly accessible
  location ~ ^/_$ {
    internal;
  }

  # Make /up look first for /up.html then /up/index.html
  location / {
    try_files $uri $uri.html $uri/index.html =404;
  }
}
